version: "3.8"
services:
  frontend:
    build:
      context: ./footnote-frontend
    ports:
      - "80:80"
  backend:
    build:
      context: ./footnote-backend
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - API_KEY=${API_KEY}
    ports:
      - "5000:5000"
